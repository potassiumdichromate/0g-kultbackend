{
  "name": "@platform/events",
  "version": "0.1.0",
  "private": true,
  "description": "Canonical event payload schemas and NATS subject constants shared by every producer/consumer.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
