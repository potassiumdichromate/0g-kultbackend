{
  "name": "@platform/sync-service",
  "version": "0.1.0",
  "private": true,
  "description": "Round 3: replaces the per-game zerodash-adapter/warzone-adapter services. Reads every Game row with integrationMode=POLLING_ADAPTER from Postgres and starts/stops a polling-adapter instance per game dynamically — adding or retiring a game's zero-touch bridge is a database row, never a new service.",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@platform/utils": "*",
    "@platform/events": "*",
    "@platform/db": "*",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.2",
    "tsx": "^4.15.6",
    "typescript": "^5.4.5"
  }
}
