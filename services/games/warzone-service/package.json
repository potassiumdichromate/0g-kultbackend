{
  "name": "@platform/warzone-service",
  "version": "0.1.0",
  "private": true,
  "description": "The per-game platform service for Warzone Warriors. Unity talks to this, not directly to save-service or 0G — this owns Warzone's save shape and gameplay-event vocabulary (mission completion), and delegates the generic save mechanic to save-service internally. Reference implementation of the per-game-service tier (architecture/00-platform-vision.md).",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@platform/utils": "*",
    "@platform/events": "*",
    "@platform/db": "*",
    "@platform/zg-client": "*",
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/cors": "^2.8.17",
    "@types/node": "^20.14.2",
    "tsx": "^4.15.6",
    "typescript": "^5.4.5"
  }
}
