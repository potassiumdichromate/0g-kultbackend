{
  "name": "@platform/identity-service",
  "version": "0.1.0",
  "private": true,
  "description": "SIWE nonce + signature -> platform JWT issuance. Drop-in compatible with the auth flow already implemented in zerodash-0g-backend and warzone-backend-0g.",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@platform/utils": "*",
    "@platform/dto": "*",
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "ethers": "^6.13.1",
    "ioredis": "^5.4.1",
    "express-rate-limit": "^7.4.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/cors": "^2.8.17",
    "@types/node": "^20.14.2",
    "tsx": "^4.15.6",
    "typescript": "^5.4.5"
  }
}
