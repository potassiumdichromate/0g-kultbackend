{
  "name": "@platform/verification-service",
  "version": "0.1.0",
  "private": true,
  "description": "Anti-cheat / TEE validation for managed saves. Ports the anti-cheat client from ZeroGCompute.js (duplicated in zerodash-0g-backend and warzone-backend-0g) into one shared implementation, called for every save-service SAVE_COMPLETED event.",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@platform/utils": "*",
    "@platform/events": "*",
    "@platform/db": "*",
    "@platform/zg-client": "*",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.2",
    "tsx": "^4.15.6",
    "typescript": "^5.4.5"
  }
}
